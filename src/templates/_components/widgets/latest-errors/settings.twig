{% import "_includes/forms" as forms %}

<div id="{{ id }}">
    {{ forms.textField({
        type: 'number',
        label: 'Count'|t('vredirect'),
        id: 'countField',
        name: 'count',
        value: widget.count,
        min: 1,
        max: 100,
        instructions: 'How many 404s should we display?'|t('vredirect')
    }) }}
</div>
